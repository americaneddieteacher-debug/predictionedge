# ibookk 백엔드 로직 전수 감사 — 버그 리포트 (심각도별)

대상: ibookk-os 프로덕션 DB의 함수·트리거·제약·RLS 전체(= 앱의 실제 백엔드 코드베이스).
방식: 각 로직을 프로덕션에서 실제 실행/공격해 재현 → 수정 → 재검증 → 테스트 데이터 완전 삭제.
(앱의 React/API 라우트 소스는 BIG 저장소라 이 세션 접근 밖 — DB 계층만 대상.)

## 🔴 CRITICAL — 돈/장부가 조용히 틀어지는 것 (전부 수정 완료)

| # | 버그 | 영향 | 수정 |
|---|---|---|---|
| C1 | **시산표가 draft/void 분개까지 합산** | 재무제표·세무 숫자 오염 (첫 draft 생기는 순간) | 054 |
| C2 | **마감된 기간에 분개 게시 가능** (draft를 미래날짜로 만든 뒤 마감기간으로 이동→게시) | 마감·신고된 재무제표가 사후에 바뀜 | 064 |
| C3 | **초과결제 은폐** (인보이스/청구서 amount_paid>total인데 'paid'로 위장) | 현금-AR 불일치가 안 보임 | 062 |
| C4 | **RAG 세무검색 함수가 호출 즉시 크래시** (삭제된 컬럼 참조) | AI 세무 Q&A 전체 불능 | 056 |
| C5 | **tax_assets 과상각 허용** (총상각액이 취득원가 초과 가능, 원가≤0, 상각기간 0=0으로나눔) | §1245/§1231 계산 오염 → **잘못된 1120-S 신고서** | 065 |

## 🟠 HIGH — 사용자 앞에서 기능이 막히거나 잘못 동작 (전부 수정 완료)

| # | 버그 | 영향 | 수정 |
|---|---|---|---|
| H1 | **온보딩 교착**: 신규 가입자가 자기 조직의 owner가 될 수 없음 | 회원가입 직후 아무것도 못 함 | 060 |
| H2 | **결제 삭제(환불) 시 상태가 paid/partial에 고착** | 환불했는데 "결제완료"로 남음 | 054 |
| H3 | **은행 조정이 차액≠0인데 '조정완료' 가능** + 차액 정합성 미검증 | 거짓 조정완료 저장 | 063 |
| H4 | **연체(overdue) 상태가 저장값이라 표류** — 마감일 지나도 pending으로 남음. 준비도 함수가 이를 놓쳐 준비도를 과대평가 | 마감 임박/연체가 UI·준비도에 안 보임 | 066 |

## 🟡 MEDIUM — 데이터 품질·보안 심화 (전부 수정 완료)

| # | 항목 | 수정 |
|---|---|---|
| M1 | anon 키로 42개 테이블/함수 노출 (은행토큰·TIN·급여 포함) | 054 |
| M2 | 로그인 없이 호출 가능한 SECURITY DEFINER 함수 6개(+search_path 미고정 8개) | 054 |
| M3 | organizations 항상-참 INSERT 정책 | 054 |
| M4 | RLS auth 호출 행별 재평가 8건 + 쓰기정책 SELECT 중복 13건 (성능) | 057 |
| M5 | pgvector가 public 스키마에 설치 | 055 |
| M6 | 미인덱스 FK 28개 | 054 |
| M7 | 급여(pay_runs/pay_run_entries/employees)·정보신고(tax_forms) 금액 비음수 방어 없음 | 067 |

## ✅ 감사했으나 문제 없던 것 (방어 장치 정상)
- 불균형 분개 거부, 게시분개 라인 불변, 게시분개 삭제 차단 → 정확히 작동
- 결제 금액 양수 강제, 분개 라인 단면·비음수 강제
- 중복 방지 unique: 계정코드/owner_basis/멤버/거래(Plaid+hash)/은행계좌
- 역분개 원자성, 연말마감 P&L 제로화·이익잉여금, 재분개
- RLS 테넌트 격리(테이블·시산표뷰·조직 가로채기 차단)
- Form 7203 검산, 1120-S 준비도, MTD 통계, 원장 잔액/월별 P&L 계산

## ✅ DB 계층 감사 완결 (2026-07-13)
프로덕션 DB의 함수·트리거·제약·RLS를 전수 감사했습니다.
- **트리거 배선 전수 확인**: updated_at 32개 + 원장 가드(균형/마감/불변/게시)·결제 재계산 전부 정상 연결
- **SECURITY DEFINER 함수**: 전부 search_path 고정 + anon 실행 차단 (미고정/노출 0)
- **RLS 커버리지**: 모든 테이블 RLS 활성 + 정책 보유 (정책 0개/RLS 꺼짐 테이블 0)
- **금액 제약**: 원장·결제·자산·급여·정보신고 전 영역 비음수/정합성 가드 완비
- **감가상각·compliance·급여·자산처분** 데이터 무결성 제약 추가 완료

## ⚠️ 이 세션에서 감사 불가 (BIG 저장소 = 네트워크 차단)
아래는 **앱 소스 코드(React/Next.js, tax-engine 순수 계산 로직)** 영역으로,
BIG 저장소 접근이 필요합니다. BIG 세션에서 `/goal` 또는 code-review로 이어서 감사하세요:
- MACRS/§179/§1245/§1231 **계산 로직 자체**(tax-engine 패키지 — DB는 입력 제약만 검증됨)
- API 라우트 입력 검증·에러 처리·인증 가드
- React 컴포넌트 예외 처리(빈 상태, 로딩 실패, null 렌더)
- tax_recommendations **생성** 흐름(AI 호출·프롬프트) — DB는 저장 정합성만 검증됨
- Plaid/Shopify/Check HQ/TaxBandits 연동 에러 핸들링

## 추가 감사: predictionedge 저장소 (이 세션 직접 접근 가능한 코드)
DB 외에 이 저장소의 정적 파일도 감사했습니다.
- **game.html (용병단연대기, 1,515줄)**: 로직 전반 견고. **발견·수정 1건** —
  손상/구버전 세이브 로드 시 알 수 없는 직업(cls)이 있으면 `CLASSES[m.cls].growth`에서
  TypeError로 **게임 전체 흰 화면**. `load()`에 sanitizeMerc 정화 계층 추가(알 수 없는
  직업 용병 제거, 숫자 필드 강제, 장비 형태 검증, 통계/자원 기본값 보정). 문법 검증 통과.
- **index.html / kit.html (PredictionEdge 랜딩·킷)**: 구독 링크가 플레이스홀더
  (`https://predictionedge.pro/sub`, index.html에 TODO 주석). 실제 Beehiiv URL로 교체 필요 —
  기능 버그는 아니나 출시 전 사용자가 교체해야 할 항목(콘텐츠 플레이스홀더).

## ✅ 앱 계층 감사·수정 완결 (2026-07-13, BIG 저장소 접근 확보 후)
BIG 저장소를 세션에 추가(add_repo)해 앱 소스를 직접 감사·수정했습니다.
**BIG PR #2** (americaneddieteacher-debug/BIG#2, 브랜치 claude/ibookk-app-security-fixes) — typecheck·test 통과.

| # | 등급 | 버그 | 수정 |
|---|---|---|---|
| A1 | 🟠 HIGH | **인가 우회**: 서버 액션이 RLS 우회 admin 클라이언트로 쓰면서 역할 미검증 → viewer도 청구서/인보이스/원장/급여/라이선스/통지서/세무 생성·수정 가능 | auth-context에 assertRole 헬퍼 + 8개 액션 파일에 DB RLS와 동일한 역할 게이트(bookkeeper/admin) |
| A2 | 🟡 MED | **overpaid 상태 UI 미반영**(DB 062): 미수금 합계·연체 판정·상태 뱃지가 overpaid를 몰라 오작동 | invoices/bills 페이지 필터·뱃지맵 보정 |
| A3 | 🟡 MED | **중복 연락처**: createBill/createInvoice가 매번 벤더/고객 새로 INSERT | 이름 기준 조회 후 재사용 |
| A4 | 🟡 MED | **비원자적 쓰기**: 라인 insert 에러 미확인 + 실패 시 라인 없는 부모 잔존 | 에러 확인 + 부모 롤백 |

### 감사 중 확인된 사실
- **B1(RAG 반환형 변경)은 앱 수정 불필요**: 소비부 인터페이스가 애초에 irc_sections를 안 읽었음 → DB(056) 수정으로 이미 정상.
- **라이브(master) 범위**: 감가상각/§1245/1120-S 엔진은 master에 **없음** — 미병합 브랜치 ibookk/hardening-ledger-foundation 전용. 라이브 앱은 부기(장부·인보이스·청구서·거래·영수증·통지서·세무Q&A/전략) 중심.
- getSessionContext는 role을 반환하나 액션들이 안 썼던 것이 A1의 근본 원인.

### 남은 것 (별도 브랜치 필요)
- ibookk/hardening-ledger-foundation 브랜치의 tax-engine 계산 로직(MACRS/§1245/§1231) 감사 — 그 브랜치에서 별도 진행.
- 마이그레이션 054~067을 claude/ibookk-os-migration-sync-oyiams 브랜치와 동기화.

